# templates/imports/python-base.yaml
# Base Python development environment

languages:
  python:
    enable: true
    uv:
      enable: true

scripts:
  test:
    exec: pytest "$@"
  format:
    exec: ruff format src/ tests/
  lint:
    exec: ruff check src/ tests/
