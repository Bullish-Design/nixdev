# templates/imports/precommit.yaml
# Pre-commit hooks configuration

pre-commit:
  hooks:
    ruff-format:
      enable: true
    ruff-check:
      enable: true

profiles:
  dev:
    module:
      pre-commit:
        enable: true
